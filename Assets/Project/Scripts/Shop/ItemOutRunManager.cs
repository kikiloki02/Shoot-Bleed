using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ItemOutRunManager : MonoBehaviour
{

    /// <summary>
    /// That script will manage the respawn of the upgrades out run
    /// 
    ///  - the store will be refilled if you've bought and there are items left to buy that haven't been spawned (only the blank spaces)
    ///  - 
    /// 
    /// </summary>
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
